# Verify: Design system & UI foundation · spec 0001 · updated 2026-09-23
_Steps derived from spec 0001 acceptance criteria. `/check verify` runs these; `/test` locks the durable ones._

## UI / manual

- [x] Load `/` (public) → the hero heading is the page's only `<h1>`; "Explore by real community need", "We carry knowledge across borders", "Insights from the frontlines", and "Active community campaigns" render as `<h2>`; community and campaign card titles render as `<h3>` → AC-1 — confirmed by reading `index.tsx`: one `<h1>` (line 104), four `<h2>` (171, 205, 237, 252), card titles as `<h3>` (182, 266). Not driven in a live browser this pass.
- [x] Inspect the hero heading in devtools → it carries `data-not-typeset` with the reason comment directly above it in source (`index.tsx`) → AC-1 — confirmed in source: `index.tsx:103-104`, comment present directly above the attribute.
- [x] Inspect the hero color blocks and the "We carry knowledge across borders" section background → colors resolve via `background-color: var(--hero-accent-1|2|3)` / `var(--hero-section-bg)`, not a raw hex class or inline hex → AC-2 — confirmed in source: `index.tsx:148` uses `bg-(--hero-accent-1|2|3)`, `index.tsx:200` uses `bg-(--hero-section-bg)`; the raw hex values only appear as the `style` object defining those custom properties (lines 19-25), which the spec's own token exception allows.
- [x] Search `index.tsx` and `__root.tsx` for `bg-[#`, `text-[#`, `border-[#`, or `bg-olive` → no matches outside the Token exceptions table → AC-2 — zero matches for the arbitrary-hex patterns repo-wide (see Commands below); `bg-olive` also absent from `__root.tsx`.
- [ ] With OS/browser "reduce motion" enabled, reload `/` → hero, section, and language-select animations show no movement or delay; the mobile nav drawer and popover open with no slide/scale transition → AC-3 — **not verified live**; needs a real browser with the OS reduce-motion flag set. Source-level support confirmed: global `@media (prefers-reduced-motion: reduce)` floor exists at `shared/ui/src/styles/globals.css:330-336` and zeroes all animation/transition durations app-wide, which by construction covers the Drawer/Popover/language-select transitions too since none are exempted from that selector.
- [x] Toggle the mobile nav trigger in the header (viewport < md) → a Drawer opens from the right showing all nav items; tapping one closes the drawer and navigates → AC-5, "give the mobile menu trigger button the handler it currently lacks" — confirmed in source: `headers.tsx` wires `DrawerTrigger`/`open={mobileNavOpen}` (lines 109-114) and each nav item's `onClick={() => setMobileNavOpen(false)}` (line 150). Not clicked in a live browser this pass.
- [x] Compare `/` (under `(public)`) and `/(protection)` in devtools → the outlet wrapper carries `data-posture="expressive"` on public and `data-posture="dense"` on protection, each resolving different `--posture-radius`/`--posture-spacing` values → AC-7 — confirmed in source: `(public)/route.tsx:11` and `(protection)/route.tsx:8` set the respective attributes; `globals.css:346-355` defines distinct token blocks for each.
- [x] Inspect the hero color blocks and the "We carry knowledge across borders" image panel → `border-radius` resolves through `--radius-4xl`; community and campaign cards resolve through `--radius-3xl` → AC-6 — confirmed: `index.tsx` uses Tailwind's `rounded-4xl`/`rounded-3xl` utilities (lines 153, 179, 222, 255, 261), which resolve through `--radius-4xl`/`--radius-3xl` defined at `globals.css:175-176`.

## Commands

- [x] `bun run typecheck` (repo root) → passes for `@pherus/ui` and `transspace` → AC-1…AC-7 (no type regressions) — passed, `FULL TURBO` cache hit, both packages clean.
- [x] `bun run lint --filter=@pherus/ui --filter=transspace` → no `no-restricted-syntax` hits for raw hex color utilities → AC-2 — the AC-2 rule itself produced zero hits; the command still exits 1 on 5 pre-existing `import/consistent-type-specifier-style` errors in `badge.tsx`, `button.tsx`, `sonner.tsx`, `utils.ts` (all untouched by this spec's diff) — unrelated to this spec, flagged as a separate pre-existing issue, not a regression.
- [x] `grep -rn "bg-\[#\|text-\[#\|border-\[#" shared/ui/src www/src` → only matches, if any, have a corresponding row in the spec's Token exceptions table → AC-2 — zero matches.

## Acceptance-criteria coverage

- AC-1 (typeset defaults, `data-not-typeset` escape hatch, one `<h1>`, no skipped heading levels) — covered by the heading-hierarchy and hero `data-not-typeset` steps above.
- AC-2 (no raw inline color/hex outside the Token exceptions table) — covered by the hero token and grep/lint steps above.
- AC-3 (shared motion module, real `motion` dependency, global reduced-motion floor) — covered by the reduced-motion manual step above. Not separately verified: automated confirmation that `headers.tsx`/`index.tsx` import from `shared/ui/src/lib/motion.ts` rather than a local copy (confirm by reading the import lines; both files import `fadeUp`/`fadeDown`/`staggerChildren` from `@pherus/ui/lib/motion`).
- AC-4 (accessibility floor in `shared/ui/AGENTS.md`, contrast measured) — the light-mode `--muted-foreground` on `--background` contrast was computed at 4.26:1 (OKLCH → sRGB → WCAG relative luminance), under the 4.5:1 floor; per the spec's own instruction this was resolved by removing `!important` from `typeset.css`'s paragraph color rule rather than changing the palette. Hit area, focus state, and color-is-never-the-only-signal are written into `shared/ui/AGENTS.md` but not mechanically checked here — that's the manual review the spec itself calls for (no test framework exists yet).
- AC-5 (Popover + Drawer primitives, CVA/`data-slot` pattern, roadmap) — covered by the mobile-nav-drawer step above; `Popover` (`shared/ui/src/components/popover.tsx`) exists but has no call site yet (no desktop dropdown/mega menu was already built to attach it to) — flagged, not a regression.
- AC-6 (shape signature radii, `signatureSpring` on confirmation moments) — radii covered by the devtools step above. `signatureSpring` is exported from the shared module but **not wired to anything**: the current UI has no save/submit/approve action (no backend yet, per this build's scope). This is an open gap, not silently skipped — wire it when the first such action is built.
- AC-7 (posture attribute + token blocks) — covered by the devtools comparison step above.
